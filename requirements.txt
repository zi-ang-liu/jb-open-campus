# Packages needed to render the executed {python} chunks.
# Installed by the shared publish workflow when python-version is set.
#
# Pinned to the versions that produced the committed _freeze/ (the same pins
# as or-book). Unpinned, CI installs whatever is newest on the day it runs,
# and a minor bump can silently redraw a figure. Bump these deliberately,
# delete the affected chapter's _freeze/ entry, re-render, and commit the
# resulting _freeze/.
jupyter==1.1.1
matplotlib==3.10.6
networkx==3.4.2
numpy==2.2.6
